import 'package:flutter/material.dart';

class SearchSportsVenue extends StatelessWidget {
  const SearchSportsVenue({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
